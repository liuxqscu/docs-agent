# Packaging Assets

Place Windows installer icon here:

- File name: `DocPulse.ico`
- Recommended sizes: include 16, 32, 48, 64, 128, 256 px layers in one `.ico`

The build script `scripts/build-release.bat` will automatically pick this icon when present.
If missing, jpackage default icon will be used.
